[project]
name = "quinn"
version = "0.1.1"
description = "Quinn — protoLabs QA engineer + release manager agent"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
